# EDA Prompt Engine

A version-controlled prompt system for generating structured EDA reports using LLMs.

## Structure
- prompts/: versioned prompt specifications
- tests/: test input summaries
- evaluations/: model outputs

## Goal
Treat prompt as a design artifact, not plain text.
